<% if @hunt.errors.any? %>
  <div class="error-explanation">
    <h2><%= pluralize(@hunt.errors.count, "error") %> prevented this hunt from being created:</h2>
    <ul>
    <% @hunt.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
